# 闭唇语音 sEMG 提词采集程序

当前版本 **v0.4.1**。默认词表为 **open / close / next / music**，可在主界面“管理词表 / 添加词”中扩展到十词或其他词数。动作统一为“嘴唇始终闭合，内部尝试发音一次，不出声”。

## 队友首次克隆：Windows安装与启动

仓库只提供源码，第一次使用需要安装依赖；不需要创建作者的 `L:\ntu_class\6008` 目录。推荐 **Windows 10/11、64位Python 3.12**，安装Python时启用Python launcher或加入PATH。安装脚本接受64位Python 3.12–3.14，但目前实际验证环境是3.12。macOS/Linux暂未作为交付平台验证，`.cmd`脚本用于Windows。

1. 克隆项目到有写入权限的本地目录，例如 `D:\projects\semg`。GitHub团队仓库使用 `upper_computer_v0.4.1` 分支，克隆后进入其中的 `upper_computer/` 子目录（本README和`start.cmd`所在目录）。目录可以改名；不要放在只读目录。
2. 双击 **`setup.cmd`**。脚本优先使用项目内已有的独立Python，否则查找`py -3.12`和PATH中的Python，在当前目录建立`.runtime`并安装运行依赖。首次安装需要网络，完成后执行导入与依赖一致性检查。
3. 双击 **`start.cmd`**。第一次可以选“模拟演练”，不接板子也可检查界面和采集流程；模拟数据不参与真实LDA训练。
4. 真实采集需板子已烧录完整采样固件、串口驱动正常，并在界面选这台电脑实际出现的端口。**COM4只是作者电脑的默认偏好，不是强制端口。** 不要让其他串口软件同时占用它。
5. 新克隆没有作者的录制和模型，因此LDA初始数据列表为空是正常的。自己采集，或另外接收完整录制目录及模型后再扫描/加载。

也可以在项目目录的PowerShell执行：

```powershell
.\setup.cmd
.\start.cmd
# 已安装后的只读环境检查，不安装软件、不连接串口：
.\setup.cmd --check
```

脚本提供`--no-pause`用于自动检查。无需激活虚拟环境，也无需修改PowerShell执行策略。升级源码后重跑`setup.cmd`可同步依赖；正在录制时先结束保存并关闭整个程序。

### 依赖清单

|文件|用途|
|---|---|
|`requirements.txt`|上位机、模拟采集、导出、LDA所需的6个直接依赖，按本机验证版本固定：PySide6、PyQtGraph、pyserial、NumPy、SciPy、scikit-learn。pip自动安装其传递依赖|
|`requirements-dev.txt`|可选测试依赖：运行依赖加pytest；普通使用不必安装|
|`requirements-analysis.txt`|可选离线绘图：运行依赖加matplotlib；主界面不需要matplotlib|
|`requirements-lock.txt`|作者Python3.12环境的完整已安装版本快照，含传递依赖与pytest；需要对齐该环境时可显式安装，并执行`pip check`|

不需要CUDA、PyTorch或TensorFlow。当前没有CNN训练。运行所需依赖与开发、历史分析依赖分开管理。

```powershell
# 开发者：不需要真实录制即可运行的软件验证
.\.runtime\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.runtime\Scripts\python.exe -m pytest -q
.\.runtime\Scripts\python.exe tests\gui_smoke.py
.\.runtime\Scripts\python.exe tests\vocabulary_gui_smoke.py
# 可选：运行离线绘图脚本前安装
.\.runtime\Scripts\python.exe -m pip install -r requirements-analysis.txt
```

`vocabulary_gui_smoke.py`可使用合成数据完成十词训练与旧格式模型兼容检查；作者的真实7/8复核在未提供私有数据时明确跳过。`lda_gui_smoke.py`、`rhythm_gui_smoke.py`和历史analysis脚本则依赖指定历史录制/模型，单纯clone并不具备这些输入；`hardware_smoke.py`还会占用实际串口。多数analysis脚本与产物也未纳入Git。

### 路径与另外分享的文件

|内容|clone后是否存在|迁移方式|
|---|---|---|
|源码、依赖清单、固件源码|提交并推送后存在|任意有写权限的本地目录均可；上位机从源码位置定位项目根目录|
|`.runtime/`、`.python/`|不存在，已忽略|在队友电脑重新安装Python并运行setup；不要直接复制虚拟环境|
|`recordings/`|不存在，已忽略|另行分享每份完整录制目录，保留metadata、samples、events、trials等文件；LDA可选择其他位置的数据目录|
|`models/`|不存在，已忽略|另外分享模型目录；加载时选择本机model.json。其训练指纹仍有效|
|`vocabulary.json`|不存在，已忽略|队友默认四词，可自行设置；若需共享同一采集词表，另行复制该配置|
|历史分析、源码ZIP、编译HEX、测试输出|大部分不在Git|按需另外提供；普通运行不依赖这些历史产物|

模型和报告中记录的作者绝对路径用于来源追溯；模型预测不需要访问旧路径。但历史报告、接手文档里的本机文件链接，在队友电脑可能无法打开。新增预测报告会记录本机选择的数据路径。

Python虚拟环境会引用创建它的Python位置，因此**移动已经安装好的项目后也应重建环境**，而不只是改项目目录名。[Python官方说明](https://docs.python.org/3.12/library/venv.html#how-venvs-work)。如提示`.runtime`损坏或引用旧位置，关闭程序后把它改名为`.runtime-old`，再运行`setup.cmd`；保留`recordings`、`models`及词表。安装脚本不会自动删除目录。

### 固件开发与发布范围

只运行上位机不需要Keil。需要编译/烧录固件时，另装Keil MDK、相应ARM编译器和STM32H7设备包，再打开已包含的`firmware/Test/MDK-ARM/Test.uvprojx`；当前工程记录ARM Compiler 6.23及Keil.STM32H7xx_DFP 4.1.3。工程源文件引用为相对路径。`firmware/prepare_firmware.py`是作者早期从上级旧工程生成副本的脚本，不是安装步骤，队友不需要运行它。

发布前需要把新的源码、脚本和说明实际加入提交并推送；只执行push不会包含未提交修改或未跟踪文件。`.gitignore`默认排除运行环境、实验录制和模型，不会因为上传源码自动分享这些内容。

## 自定义词表与模型兼容（v0.4.0）

- 在“本次采集”中点击“管理词表 / 添加词”，输入词并点击添加，勾选本批要采集的词，最后保存。英文自动转小写；词不能重复，`rest` 保留给静息。每轮所有勾选词各一次，轮内随机。例如十词、每词10次，就是100个试次。
- 本机词表和勾选状态保存在 `vocabulary.json`，重启后保留；正在采集或收尾时不能改词表。每份新录制独立保存 `words`、`label_map` 和词序。已有录制、模型不随主界面词表变化；不同录制的数字标签可能不同，LDA按词语名称对齐。
- LDA根据选中的训练数据确定类别，至少两个词，不再固定四词。自动划分仍要求每个选中词在训练集至少2次、测试集至少1次；手动指定允许测试录制与训练词表不同。
- 新模型使用 `semg_lda_v2` 格式，保存自己的词表；仍能加载原GUI生成的四词 `semg_lda_v1` 模型。早期分析脚本的旧JSON格式仍不兼容。
- 十词模型可以测试只含四词的录制，但预测仍在全部十词中选择，不会因测试标签缩小输出范围。四词模型也能测试包含新词的录制；新词会被强制判成模型已知词，并在报告中标注“未训练”，不会自动拒识。
- 结果同时显示全部准确率、已知词准确率、未训练词次数及未测试的模型类别。未训练词计入全部准确率的错误，不会被悄悄排除。输入仍须是本程序支持的真实1 kHz、含事件标记的录制。
- 添加词不会自动更新已有模型，需要采集对应数据后重新训练。CNN暂未加入，无新增依赖，无需重烧固件。

## 现在如何使用

本机独立 Python 环境已经安装好，**双击本目录的 `start.cmd` 即可启动**，不需要先安装依赖。程序运行在电脑上；Keil 仍用于给 STM32 编译和烧录，两者通过串口协议衔接。

1. 在 SerialPlot 中断开串口连接，同一端口不能同时由两个程序占用。
2. 已烧录新固件时，选择 **完整采样 · 二进制 v1**（默认）。旧三列文本固件才选择对应联调模式。
3. 选择 **COM4**，点击“连接 / 检查数据”。2026-09-23 实测有效数据在 COM4（USB-SERIAL CH340）；COM3 是另一 USB 串口，当时未收到数据。拔插后端口号可能变化，请按实际情况选择。
4. 等待“数据就绪”，检查三条波形及接收速率，新固件约为 **1,000 点/秒**。
5. 填写受试者编号、电极位置（含参考电极位置）、Session 和备注。每次重新贴电极或换一天，建议换 Session。
6. 点击“开始采集”。默认每词 10 次，四词共 40 次，每轮随机顺序。初始静息 10 秒，每试次准备 1.5 秒、内部发音 2 秒、休息 2 秒，总计约 230 秒，另有保存收尾时间。
7. 可打开参与者全屏提词，按 Esc 返回操作界面。受试者无需在动作期间按键。
8. 全部完成后自动保存；也可“结束并保存”。“打开本次数据文件夹”可以直接查看结果。

**旧三列文本模式仅用于联调：它只有约 100 行/秒数据，没有设备采样序号。**2026-09-24 的 S05 新固件记录已验证完整 1 kHz 数据、连续采样序号和阶段应答；标记仍有约 20 ms 块边界粒度，不能当作屏幕实际呈现时刻的精确测量。

也可选择“模拟演练”体验界面：该模式不打开任何 COM 口，数据全部合成，单独保存到 `simulated`。模拟模式能跑通分类数据接口，并不证明真实信号能区分四个词。

## 操作行为

v0.3.0 已集成 **LDA 训练与模型测试**，入口在主界面右上角。支持选择受试者、批次和具体试次，按轮次/录制/受试者划分或手动指定训练与测试；一键训练保存模型，再加载模型测试已保存的数据。操作步骤、格式与结果解释见 [LDA使用说明](LDA使用说明.md)。本机依赖已安装，重启即可使用，无需重烧 STM32。

- **准备**：看清单词，保持放松；**动作**：保持闭唇，内部尝试发音一次；**休息**：放松。准备阶段与休息阶段在事件中分开记录。
- **当前作废 + 补录**：保留原始数据，标记该试次作废，在队尾追加新 trial_id 的补录，保存 repeat_of 关系。重复点击不会反复追加。
- **暂停**：进行中的试次标记 interrupted 并排入补录，接收和连续保存仍继续。恢复从下一个试次开始，不拼接动作前后片段。
- **结束**：当前未完成试次标为 interrupted；保留已完成试次。收尾等待约 0.75 秒以保存最终事件应答和在途数据。
- **异常**：有效数据中断、设备重启、正式记录期间采样缺口、设备故障或事件应答超时会终止批次并写错误状态。关闭正在记录的窗口时，先结束保存，再次关闭即可退出。
- **波形**：完整采样/模拟模式显示原始 ADC、20–400 Hz 带通加 50 Hz 陷波（Q=30）、陷波后的 200 ms 平均绝对值包络。采用连续状态的因果滤波，重新连接时清空状态，开始记录不会打断滤波。旧三列文本模式继续显示板端输出，不对约 100 行/秒的数据套用 1 kHz 滤波器。画面仅显示最近约 5 秒，保存包含全程。
- **降噪与保存**：2026-09-24 加入实时 50 Hz 陷波，无需重新烧录板子；关闭并重新运行 `start.cmd` 生效。原始 ADC 和串口字节不覆盖，metadata 记录显示滤波参数。`export_trials.py` 仍导出原始片段，后续 LDA 必须显式应用预处理；不要把导出的原始片段误认为已陷波。以前的完整采样数据也可重新处理。当前仅陷波 50 Hz，未自动去除其所有谐波。

## 开始前如何看波形（2026-09-24）

三条图的纵轴默认固定：原始 ADC 0–65535，滤波波形 ±10000，包络 0–5000 counts。点击“适配当前波形并锁定”仅调整一次，并固定后续量程；可用“手动量程”分别设置上下限。超出显示范围会提示，不自动缩放；显示越界不等于 ADC 削顶。调整量程只影响显示，不改变采样、滤波、记录或标签。

放松时先看原始信号有无突然跳变或量程端点削顶，再看滤波波形是否仍有很强的持续振荡；用包络比较“放松—内部发音一次—再放松”是否可重复增强和回落。近 1 秒滤波 RMS 是绝对数值参考，不是自动合格阈值。包络平稳不代表噪声低，强而稳定的干扰也会形成平稳包络。先放松观察约 5–10 秒，再做几次练习，比较时保持相同量程；不要为了获得平坦图形而反复缩小或放大量程。截图或单个 RMS 数值不能确认信号来源、类别可分性或硬件无异常。

若程序显示“已暂停”，则本批已开始，暂停期间仍持续保存；更新程序前应先结束并保存，再关闭重启。

## 输出文件

### 静息节律提示（v0.2.0，2026-09-24）

重启 `start.cmd` 后，在波形上方可看到“静息节律”提示。选择真实的“完整采样 · 二进制 v1”，连接后不发音、尽量不吞咽或移动，放松约 **11 秒**（半秒过渡保护，再累积连续 10 秒）。检测到重复脉冲后显示“稳定的疑似心搏节律”、约多少次/分、5–30 Hz 分支内脉冲峰峰值；幅度单位仍为 ADC counts。已暂停时也能检查。正常提词的准备、发音和短休息阶段暂停判断，不把不同休息段拼起来。

这是从喉部 sEMG 中寻找节律的实验功能，没有同步 ECG/PPG 验证。**检出不代表接线或肌电质量合格，未检出也不代表线路断开**；不会阻止开始采集，也不自动作废试次。削顶、明显基线移动、数据不连续或超过 1.5 秒无新数据时不保留旧的肯定提示。旧固件及模拟模式不启用。

计算使用独立预览分支，每秒最多分析一次；主显示仍为 20–400 Hz + 50 Hz 陷波，原始保存不受影响。metadata 保存算法配置和软件版本；本版不逐拍记录候选事件。固定纵轴及原有采集流程保留，无需重烧 STM32。

当前有效性证据是 12:34 录制静息数据回放和合成反例测试，尚未进行新的在线人体采集验证。版本与备份见 `VERSIONING.md`。

## 采集文件

真实连接数据位于 `recordings/<subject>/session_NNN/<时间和唯一后缀>/`；模拟数据位于 `simulated/`。时间和随机后缀保证不会覆盖旧批次。

| 文件 | 内容 |
|---|---|
| samples.csv | 连续原始数据，含 host_row_idx、device_sample_idx、接收批次和电脑接收时间；旧固件的设备序号留空 |
| events.jsonl | 提示阶段、暂停/结束、软件时刻、标记发送及设备应答；真实显示时刻未硬件测量 |
| trials.jsonl | 各试次的词语、标签、完成/作废/中断状态、补录关联 |
| frames.jsonl | 接收批次记录，二进制模式另记录帧号和采样区间 |
| metadata.json | 受试者/电极/动作条件、时长、初始随机顺序、版本、采样率、错误及样本数 |
| transport.bin | 原始串口字节；文件开头包含开始保存时尚未解码的缓冲前缀 |

单位为 **ADC counts**，没有将未知模拟增益假设为已知并转换成 µV。接收时刻不是采样时刻；host_row_idx 也不是设备采样序号。旧固件 metadata 的 sampling_rate_hz=100 指串口名义数据行率，板上 ADC 名义触发率仍为 1000 Hz。

`status=recording` 的遗留元数据表示上次可能崩溃或未正常收尾，不能自动当作完整记录。数据每约 1 秒刷新文件缓冲，事件逐行刷新；这不等同于断电级持久性保证。

## 完整采样固件副本

项目入口：`firmware/Test/MDK-ARM/Test.uvprojx`。

已生成：`firmware/Test/MDK-ARM/Test/Test.hex`。本机 Keil 编译结果在 `firmware/build.log`：**0 Error(s), 0 Warning(s)**。

原始工程 `L:\ntu_class\6008\code\test_lvbo\Test\Test` 未改动。副本已编译，用户已烧录新固件；2026-09-24 的 S05 数据验证了 1 kHz 完整传输和双向标记应答，详细结果见 `analysis/20260924_S05_101033/数据质量结论.md`。物理显示时刻与 ADC 的精确同步仍未独立校准。

副本保留原工程 ADC1/PA7、TIM3 和 USART1 配置，改为：

- 1 kHz 单通道 uint16 原始值，每半块 20 点，即约每 20 ms 发送一个二进制帧。
- 115200 / 8N1；采样序号、帧号、CRC16、故障标志。
- UART DMA 发送队列；ADC 和 UART DMA 数据放入 SRAM D2，配置 scatter file 并在 SystemInit 启用 D2 SRAM 时钟。
- ADC 校准、ADC 错误中断；事件标记 M 与设备信息 I 双向命令。
- 不发送板上数字滤波及包络列，电脑负责显示滤波；完整保留原始数据。

切换时用 Keil 打开上述**副本项目**，编译、Download，并确认运行；上位机选择“完整采样 · 二进制 v1”。二进制输出不能沿用 SerialPlot 原来的三列文本配置。

**双向命令需要 UART RX 接线：USB 串口模块 RX 接 STM32 PB14(TX)，模块 TX 接 PB15(RX)，共地，并确认电平匹配。**现有三列输出仅证明板子到电脑方向可通，不证明反方向已接通。先前若只接了发送方向，需要检查这根返回线。上位机必须收到 INFO 和标记 ACK 才会显示正式模式“数据就绪”。

若需回到原来的 SerialPlot 调试流程，重新用原项目烧录旧固件，并切回三列文本设置。

### 时间标记的准确含义

设备 ACK 中记录的是最近完成的 20 点块边界，量化约 20 ms，另有串口/USB传输和调度时延。文件保存了电脑发送、接收和 GUI 更新提交时刻，定期标记用于检查链路；当前版本**未实现跨时钟漂移拟合，也未标定屏幕实际亮出提示的时刻**。因此使用设备标记切出的区间是近似提示区间，不是真实肌肉动作起止。

正式批量采集前，应先验证数分钟连续序号、零故障和稳定接收速率，再用少量闭唇动作回放检查标签位置。必要时根据实际动作调整准备/动作/休息时长。已有录制显示组内可分类信息，但跨录制可靠性仍未解决。

## 导出试次，衔接后续 LDA

在此目录运行：

```powershell
.\.runtime\Scripts\python.exe export_trials.py "完整采样记录目录"
```

输出在该批次的 `cue_windows/`：每个有效试次一个 `.npz`，包含 raw_ch0、sample_idx、label_id、word、fs、trial_id，另有 manifest.csv 记录每次是否导出及排除原因。

导出器要求完整采样序号、完成状态、动作开始/结束的设备 ACK、无缺点，标记往返不超过 100 ms；这是首版保守筛选阈值，不代表实际显示误差一定小于该值。拒绝旧固件的接收时间伪对齐。模拟记录必须显式加 `--allow-simulated`；目标导出目录已存在则拒绝覆盖。

当前LDA窗口直接读取完整录制，无需先运行导出器；它使用四段log RMS及log平均绝对差分共八个特征。按轮次、录制或受试者分组划分训练/测试，不能随机拆同一动作的重叠窗口。导出器是供独立离线分析使用的可选工具。

## 已完成的验证

- `tests/test_core.py`：11 项测试通过，包括碎片帧、CRC 错误恢复、采样缺口和重启、计数回绕、随机词序、暂停/补录、时间标记与旧格式保存。
- `tests/gui_smoke.py`：真实 Qt 界面 + 模拟二进制流 + 连续保存 + 作废补录 + 导出，通过；四个有效片段，作废试次被排除。
- `tests/hardware_smoke.py`：COM4 当前固件实机读取、界面、暂停/补录、保存通过；保存 500 行。该测试为自动链路测试，没有受试者按词完成动作，**不得用于训练**。
- Keil：固件副本编译 0 错误、0 警告，map 文件确认 DMA 缓冲位于 0x30000000 区域。
- 中文界面与全屏提词已通过程序截图检查。

测试报告和截图在 `tests/output/`，与正常实验目录分开。测试时间和实际样本数见对应 JSON 报告。

复现软件测试：

```powershell
.\.runtime\Scripts\python.exe -m pytest tests/test_core.py -q
.\.runtime\Scripts\python.exe tests/gui_smoke.py
```

硬件测试会占用 COM4，需先断开其他程序，再按需运行 `tests/hardware_smoke.py`。

## 环境与文件位置

源码在 `semg/`；电脑串口读写使用 pyserial 的独立 I/O 线程，界面使用 PySide6，绘图使用 PyQtGraph。与初始方案中 QSerialPort 的差别仅在串口库，线路和协议不变。

作者电脑的`.runtime/`是本程序的虚拟环境，底层CPython在`.python/`；队友新安装时底层Python可位于其正常安装目录，不要求存在`.python/`。本机原有Anaconda的Qt DLL冲突通过使用独立环境规避。`requirements-lock.txt`记录完整环境快照，`requirements.txt`为固定版本的运行依赖。当前测试运行时是Python 3.12.13。

重新安装时可使用 `setup.cmd`；迁移到另一台电脑时建议先安装独立的 64 位 Python 3.12，再重建运行环境，不直接搬用包含绝对解释器路径的虚拟环境。
